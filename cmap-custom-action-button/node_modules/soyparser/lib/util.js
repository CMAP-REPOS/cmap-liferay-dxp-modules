"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Parses a template name, so something like `.render`
 * or `SomeNamespace.Thing`.
 */
function toTemplateName(segments) {
    const namespace = segments
        .slice(0, segments.length - 1)
        .join('.');
    return {
        name: segments[segments.length - 1],
        namespace: namespace || null
    };
}
exports.toTemplateName = toTemplateName;
/**
 * Joins the left item with the right item, or an array of items
 */
function reverseJoin(left, right) {
    if (Array.isArray(right)) {
        return [left, ...right];
    }
    return [left, right];
}
exports.reverseJoin = reverseJoin;
